# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/adtu_confessions"

# Admin Credentials
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="your-secure-admin-password-here"

# JWT Secret (generate a secure random string)
JWT_SECRET="your-jwt-secret-key-here"

# Instructions:
# 1. Copy this file to .env
# 2. Replace the DATABASE_URL with your actual PostgreSQL connection string
# 3. Set a secure admin password (minimum 8 characters)
# 4. Generate a secure JWT secret (32+ random characters recommended)
# 5. Run `npm run db:push` to initialize the database schema
