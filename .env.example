# Database Configuration
# Use Neon, Supabase, or PlanetScale for production PostgreSQL
DATABASE_URL="postgresql://username:password@hostname:5432/database_name"

# Admin Credentials (change these for production)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="your-secure-admin-password-here"

# JWT Secret (generate a secure random string - 32+ characters recommended)
JWT_SECRET="your-jwt-secret-key-here"

# Production Environment
NODE_ENV="production"

# Instructions for Deployment:
# 1. Copy this file to .env.local (for Vercel) or .env (for other platforms)
# 2. Sign up for a free PostgreSQL database at:
#    - Neon: https://neon.tech (recommended for Vercel)
#    - Supabase: https://supabase.com
#    - PlanetScale: https://planetscale.com
# 3. Replace DATABASE_URL with your actual connection string
# 4. Set a secure admin password (minimum 8 characters)
# 5. Generate a secure JWT secret using: openssl rand -base64 32
# 6. In production, add these as environment variables in your hosting platform
# 7. Run `npm run db:push` to initialize the database schema
